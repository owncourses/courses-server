vich_uploader:
    storage: file_system
    mappings:
        covers:
            uri_prefix: /assets/images/course
            upload_destination: '%kernel.project_dir%/public/assets/images/course'
            namer:
                service: vich_uploader.namer_property
                options: { property: 'title', transliterate: true}
